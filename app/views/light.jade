extends layout

block content
  each room in config.rooms
    each light in config.lights
      if light.id == lightID
        if light.roomID == room.id
          +controlCard(room.name + ' ' + light.name, 'light_' + light.id, '/light/' + light.id)